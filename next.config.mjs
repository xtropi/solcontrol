/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    unoptimized: true,
  },
  output: "export",
  // basePath: "/xtropi.github.io",
  assetPrefix: "/solcontrol",
};

export default nextConfig;
